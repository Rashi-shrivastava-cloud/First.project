name = "my name  is rashi"
print(name.find("  ")) # to count double space in a string we will use find functions ...
print(name.find("name"))
print(name.replace("name" , "nickname"))
# strings are immutable which means that you cannot change them by running functions on them
print(name)
